# =============================================================================
# StyleME Beta (formerly StyleAI) - Dependencies
# Archived beta version - Electron desktop app
# Project: https://github.com/OpenZeg/styleme-beta
# =============================================================================

# Core runtime dependencies (from package.json)
express@^4.21.2
cors@^2.8.5

# Development dependencies
electron@^35.0.0
electron-builder@^25.1.8

# Optional / Future AI extensions (if you add Python components later)
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# transformers
# pillow
# opencv-python

# Notes:
# - This is primarily a JavaScript/Electron project.
# - Run `npm install` to install everything.
# - To rebuild native modules: npm run postinstall
# - For full installation: npm run build

# How to install:
# npm install -r requirements.txt   # (Node doesn't use this directly)
# Use: npm install
